---
---
<div class="hero">
	<div class="hero-content">
		<h1>Sejarah <span class="text-gradient">Keraton Jawa</span></h1>
		<p class="tagline">"Kagungan Dalem Karaton, Warisan Luhur Tanah Jawi"</p>
		<div class="scroll-indicator">
			<span>Mangga Mirsani</span>
			<div class="arrow">↓</div>
		</div>
	</div>
</div>

<style>
	.hero {
		height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		background-image: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url('/hero-bg.jpg');
		background-size: cover;
		background-position: center;
		background-attachment: fixed;
		position: relative;
	}
	.hero-content {
		text-align: center;
		padding: 2rem;
	}
	h1 {
		font-size: clamp(3rem, 8vw, 6rem);
		font-weight: 700;
		line-height: 1.1;
		margin-bottom: 1rem;
		font-family: 'Javanese Text', serif;
	}
	.text-gradient {
		background-image: linear-gradient(45deg, #D4AF37, #FFD700);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.tagline {
		font-size: clamp(1.2rem, 3vw, 2rem);
		color: #D4AF37;
		font-style: italic;
		margin-bottom: 3rem;
	}
	.scroll-indicator {
		position: absolute;
		bottom: 2rem;
		left: 50%;
		transform: translateX(-50%);
		color: #D4AF37;
		text-align: center;
		animation: bounce 2s infinite;
	}
	.arrow {
		font-size: 2rem;
		margin-top: 0.5rem;
	}
	@keyframes bounce {
		0%, 20%, 50%, 80%, 100% {
			transform: translateY(0);
		}
		40% {
			transform: translateY(-20px);
		}
		60% {
			transform: translateY(-10px);
		}
	}
</style>